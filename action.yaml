apiVersion: automation.cloudbees.io/v1alpha1
kind: action
name: mlcb action 001
inputs:
  string-val:  # id of input
    description: 'sample string'
    required: false


runs:
  using: composite
  steps:
    - id: action-step1
      name: action step 1 (action step name)
      uses: docker://golang:1.20.3-alpine3.17 
      shell: sh
      run: |
        echo "Hello there!" 
    - id: action-step2
      name: action step 1 (action step name)
      uses: docker://golang:1.20.3-alpine3.17 
      shell: sh
      run: |
        echo "Hello there!" 
